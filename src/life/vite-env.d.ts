/// <reference types="vite/client" />
// Enables `import.meta.glob` typing for the gallery folder loader
// (gallery.ts). Scoped to /life; the rest of the app uses relative
// asset imports and does not need this.
